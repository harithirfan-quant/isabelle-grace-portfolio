/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // EDIT THIS: add remote image domains here if you host avatar/screenshots externally
    remotePatterns: [],
  },
};

export default nextConfig;
